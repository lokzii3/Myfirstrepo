apiVersion: v1
kind: Service
metadata:
  name: my-node-service
  labels:
    app: my-service
spec:
  selector:
    app: my-game
  type: NodePort
  ports:
    - port: 80 # service port
      targetPort: 80 
      nodePort: 30082    