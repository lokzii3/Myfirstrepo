apiVersion: v1
kind: Service
metadata:
  name: my-cluster-service
  labels:
    app: my-service
spec:
  type: ClusterIP
  selector:
     run: nginx 
    
  ports:
    - port: 80
      targetPort: 80      
    
